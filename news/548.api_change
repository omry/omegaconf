OmegaConf.to_container now takes a `structured_config_mode` keyword argument, which replaces the `exclude_structured_configs` argument. Setting `structured_config_mode=SCMode.DICT_CONFIG` causes `to_container` to not convert structured config objects to python dicts (it leaves them as DictConfig objects).
