Direct assignment and typed-container mutation now warn when they implicitly convert a value to another type. Use ``OmegaConf.update()`` for explicit conversion; structured-config assignment is unchanged.
